name: client

services:
  app-client:
    build:
      context: .
      dockerfile: .deploy/Dockerfile

    networks:
      -  content_host_network
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: ${NODE_ENV}
      VITE_BASE_URL: ${VITE_BASE_URL}

networks:
  content_host_network:
    external: true

#volumes:
  #app_data:
